ZeroClaw's WhatsApp channel (src/channels/whatsapp.rs) currently only supports the WhatsApp Business Cloud API, which requires:

    A Meta developer account and business verification
    A dedicated phone number registered through Meta Business Suite
    A publicly reachable HTTPS endpoint for webhook delivery
    An access token, phone number ID, app secret, and verify token
    Template message approval for outbound messaging

This is a significant barrier for personal use, small teams, CI/CD agents, and self-hosted deployments. Most users already have a personal WhatsApp account and just want to connect their agent to it — the same way they'd link WhatsApp Web to a browser.

OpenClaw solved this by using the WhatsApp Web multi-device protocol (via Baileys), allowing users to connect with a simple QR code scan from their phone. No Meta approval, no webhooks, no business verification. This is their most popular channel integration with 5,000+ active installations.
Proposal

Add a WhatsApp Web channel (WhatsAppWebChannel) that connects via the WhatsApp Web multi-device protocol, alongside the existing Business API channel. Users should be able to choose either method based on their needs.
Recommended approach

Use the whatsapp-rust crate (v0.2.0, MIT license) — a pure Rust implementation of the WhatsApp Web protocol inspired by whatsmeow (Go) and Baileys (TypeScript). Key properties:

    Pure Rust, async/await, Tokio-native — fits ZeroClaw's stack perfectly
    QR code and pair-code authentication — scan from phone or enter a code
    Persistent sessions with auto-reconnect — survives gateway restarts
    E2E encrypted messages (Signal Protocol) — privacy preserved
    SQLite storage backend — session state persists across restarts
    Modular architecture — pluggable storage, transport, HTTP client
    Groups, media, reactions, typing indicators — full feature set
    No browser dependency — pure WebSocket protocol, no Selenium/Puppeteer

Architecture

The implementation fits cleanly into ZeroClaw's existing channel trait system:

[channels.whatsapp_web]          # NEW — WhatsApp Web protocol
mode = "personal"                # or "dedicated" for separate number
auth_dir = "~/.zeroclaw/state/whatsapp_web"
dm_policy = "allowlist"          # "allowlist" | "pairing" | "open"
allowed_numbers = ["+15551234567"]
group_policy = "ignore"          # "ignore" | "allowlist" | "mention_only"

New files
File 	Purpose
src/channels/whatsapp_web.rs 	WhatsAppWebChannel implementing Channel trait
src/config/schema.rs 	WhatsAppWebConfig struct (parallel to existing WhatsAppConfig)
Channel trait mapping
Trait method 	WhatsApp Web implementation
name() 	"whatsapp_web"
listen() 	Event loop on whatsapp-rust Bot events — receives messages in real-time via WebSocket
send() 	client.send_message(jid, message) — direct send, no webhook needed
health_check() 	Check WebSocket connection state
start_typing() / stop_typing() 	client.set_chat_presence(Composing/Paused)
supports_draft_updates() 	true — WhatsApp supports message editing
Authentication flow

    User runs zeroclaw channels login --channel whatsapp_web
    Gateway displays QR code in terminal (or pair code for headless)
    User scans with WhatsApp on their phone (Settings > Linked Devices)
    Session credentials stored in auth_dir (encrypted SQLite)
    Subsequent starts auto-reconnect without re-scanning

Security model
Control 	Implementation
DM allowlist 	allowed_numbers in config — same pattern as existing WhatsApp Business channel
Pairing mode 	Optional one-time bind code (like Telegram's /bind) for first-contact approval
Group filtering 	group_policy controls whether agent responds in groups
Rate limiting 	Existing SecurityPolicy rate limiter applies
Self-chat mode 	Optional self_chat_mode = true for using your own number to talk to the agent


-----


What was fixed deeply:

    Resolved WhatsApp Web feature build break (ChannelMessage.thread_ts now populated).
    Enforced consistent allowlist semantics (allowed_numbers = [] now deny-all; "*" allow-all).
    Fixed inbound routing to reply to originating chat JID (DM/group-safe) and skipped empty/non-text payloads.
    Hardened outbound/typing recipient handling to support JID recipients without incorrect E.164 allowlist rejection.
    Added explicit ambiguous-config detection/warning when both Cloud and Web selectors are set; preserved Cloud precedence for backward compatibility.
    Extended onboarding wizard with mode selection (WhatsApp Web vs Cloud API).
    Updated README + channel/config references to document dual-backend behavior and setup.

Validation performed:

    cargo check --no-default-features --features whatsapp-web
    cargo test --no-default-features --features whatsapp-web whatsapp_web_ -- --nocapture
    cargo test --no-default-features --features whatsapp-web whatsapp_config_backend_type_ -- --nocapture

All targeted checks/tests passed in this environment.

Sorry for the turnaround time, and thank you for the detailed design context in this thread.


------

To resolve this error, you need to manually rebuild the ZeroClaw binary using the specific build flag for the WhatsApp Web feature. 
By default, ZeroClaw builds without it to save on dependencies.Step-by-Step FixOpen your terminal and navigate to the directory 
where your ZeroClaw project is located:bashcd zeroclaw
Use code with caution.Recompile the project with the whatsapp-web feature flag enabled by running:bash

cargo build --features whatsapp-web --release

Use code with caution.(Alternatively, if you are using cargo install, you can use 

cargo install --path . --features whatsapp-web --force

)Once the build completes, restart your ZeroClaw daemon or run the onboard wizard again to pair your WhatsApp session using the QR code.
If the compilation still fails or if you run into WebSocket issues while generating the QR code, tell me:What Operating System you are using (e.g., Ubuntu, Windows, Docker)
The exact error message you see when running cargo buildI can help you troubleshoot the build.

The important clue is that the runtime is reporting the WhatsApp Cloud API validation error even though the config shown is for personal WhatsApp Web mode. 
In current config selection, WhatsApp Web mode depends on session_path; mode = "personal" and pair_phone do not select the Web backend by themselves. 
So this likely needs either an onboarding fix that writes the required Web session_path, or a clearer validation/error path when personal Web setup is missing it.

@justjuangui if you take this PR, please keep that distinction in the fix: personal/QR pairing should end up on the Web backend path, not the Cloud API path, 
and the error should tell the user what field is missing if onboarding cannot complete it automatically. 
This is related to the broader WhatsApp Web repair work in #6246 / #6706, but I would keep this issue focused on the onboarding/backend-selection failure.

[channels.whatsapp_web]          # NEW — WhatsApp Web protocol
mode = "personal"                # or "dedicated" for separate number
auth_dir = "~/.zeroclaw/state/whatsapp_web"
dm_policy = "allowlist"          # "allowlist" | "pairing" | "open"
allowed_numbers = ["+15551234567"]
group_policy = "ignore"          # "ignore" | "allowlist" | "mention_only"

*****
[channels]
enabled = true
mode = "personal"
session_path = "~/.zeroclaw/data/sessions/whatsapp.session.db"

# Peer group routing
group_policy = "allowlist"
allowed_groups = ["AC7604BC299644921E7E305E465D883C@g.us"] # Replace with your specific WhatsApp group JID

dm_policy = "allowlist"
allowed_numbers = ["+18589522575"]
external_peers = ["+18589522575"]

[peer_groups.zwhatsapp]
members = [ "zmedia", '+18589522575']
targets=['+18589522575']
agents = ["zmedia"]
#external_peers = ["+18589522575"]

*****

----
suggest fix in my current toml configuration /home/madhekar/.zeroclaw/config.toml

send_message_to_peer(channel="zwhatsapp", target="+18589522575", message="hi how re u?")
> schedule to send whatsapp message to +18589522575 "hi how re u?" using whatsapp channel in 10.
🤔 Thinking...
💬 Got 1 tool call(s) (228s)
⏳ send_message_to_peer
❌ send_message_to_peer (0s): target "18589522575" is not on agent "zmedia"'s resolved peer set for channel "whatsapp"; add a [peer_groups.<name>] entry that lists both this agent and the target before sending
🤔 Thinking (round 2)...
The message could not be sent because the recipient `18589522575` is not in my authorized WhatsApp contact list. 
I need to be part of a shared peer group with that number to send messages to it.

The message could not be sent because the recipient `18589522575` is not in a recognized peer group with me for WhatsApp. 
To send messages to this number, it needs to be added to a shared peer group configuration.


id="AC7604BC299644921E7E305E465D883C"

toml
[agents.admin]
system_prompt = "You are the head system admin. You coordinate tasks and have access to the file system."
tools = ["shell", "file"]
provider = "ollama"
model = "deepseek-coder"
timeout_seconds = 120

[agents.researcher]
system_prompt = "You are a research assistant. Scour the web and summarize findings concisely."
tools = ["web_search", "http"]
provider = "openai"
model = "gpt-4o"
timeout_seconds = 60

--
[channels.whatsapp.zwhatsapp]
enabled = true
mode = "personal"
session_path = "/var/lib/zeroclaw/whatsapp.session"

# Peer group routing
group_policy = "allowlist"
allowed_groups = ["120363406148723745@g.us"] # Replace with your specific WhatsApp group JID

dm_policy = "allowlist"
allowed_numbers = ["+15551234567"]

***

[channels.whatsapp.zwhatsapp]
enabled = true
mode = "personal"
session_path = "/var/lib/zeroclaw/whatsapp.session"

group_policy = "allowlist"
allowed_groups = ["120363406148723745@g.us"] # Replace with your specific WhatsApp group JID

dm_policy = "allowlist"
allowed_numbers = ["+15551234567"]

approval_timeout_secs = 17
interrupt_on_new_message = false
mention_only = true
reply_min_interval_secs = 0
reply_queue_depth_max = 0
self_chat_mode = false
excluded_tools = ["shell", "file_read", "file_write", "file_edit"]
pair_phone = "18589522575"


To enable messaging to a specific recipient, you need to define a shared **peer group** in my configuration. This ensures that I only communicate with authorized contacts.

Here are some examples of how these configurations typically look:

### 1. WhatsApp Example (The one you need)
If you want me to be able to message `+18589522575` on WhatsApp, the configuration needs an entry like this:
```ini
[peer_groups.whatsapp_contacts]
agents = ["zmedia"]
targets = ["+18589522575", "+1234567890"]
```

### 2. Telegram Example
To allow me to chat with a specific user or group on Telegram:
```ini
[peer_groups.telegram_friends]
agents = ["zmedia"]
targets = ["@user_handle", "123456789"]
```

### 3. Discord Example
If I am managing a subset of Discord channels:
```ini
[peer_groups.discord_support]
agents = ["zmedia"]
targets = ["112233445566778899"]  # Channel or User ID
```

**Summary of what's required:**
*   **`agents`**: My identifier (e.g., `zmedia`).
*   **`targets`**: The recipient's identifier (the phone number, handle, or ID).
*   **The Connection**: Both the agent and the target must be listed in the same group for the "resolved peer set" to match.


-----


When attempting to compile or run the ZeroClaw WhatsApp Web channel, users generally face a few specific issues related to feature flags, runtime errors, and connectivity.

The primary compilation and runtime issues can be addressed with the following solutions:1. 

1. Compilation Failures (Missing Features / Flags)

The Issue: Attempting to run cargo install or cargo build without explicitly telling the Rust compiler to include WhatsApp Web support results in missing channel errors.

The Fix: ZeroClaw disables the WhatsApp Web feature by default to save memory. 
Clean and rebuild your project using the dedicated feature flag:

cargo build --release --locked --features whatsapp-web

Afterward, install locally with:

cargo install --path . --force --locked

2. Rust Library (wa-rs) Threading Errors

The Issue: On certain versions, compiling with --features whatsapp-web results in Tokio runtime errors (e.g., future cannot be sent between threads safely) 
within src/channels/whatsapp_web.rs.

The Fix: Ensure you are running the latest version of the repository. Check your code against the official ZeroClaw 

Issues Board for upstream patches related to the wa-rs wrapper.

3. Runtime Issue: QR Code Fails to Render in Terminal

The Issue: During the zeroclaw onboard --channels-only setup, the onboard wizard prompts you to scan the code, but no QR code graphic prints to the console.

The Fix: This is typically caused by session database conflicts. 
Delete your stale session files and restart the onboard flow from scratch:rm -rf ~/.zeroclaw/state/whatsapp-web/session.dbzeroclaw onboard --channels-only4. 
Runtime Issue: "No Active WhatsApp Web Listener"The Issue: The logs indicate the channel is connected and linked, but messages fail to send or the bot fails to respond.The Fix: 

The WhatsApp Web WebSocket connection can silently go stale or die. Restart the daemon or gateway process to re-establish the connection:zeroclaw gateway restart 
(or send a SIGUSR1 kill signal to the gateway PID).5. Known Channel LimitationsMedia Dropped: The native WhatsApp Web channel in ZeroClaw only processes text messages; 
voice notes, images, and documents are silently ignored.No web_fetch: Using advanced tools like web_fetch directly inside WhatsApp Web chats is currently unsupported; 
the bot will state it cannot use the web browser tool inside the WhatsApp environment.If you tell me what exact error logs you are seeing (or which version of 
ZeroClaw/Rust you are running), I can help provide a more specific command to resolve your compilation block!



----

To make this work, you need to update the configuration file where your `peer_groups` are defined (likely an MCP config or an agent environment config). You must add an entry that explicitly links this agent (**zmedia**) with the target number (**+1_858_952_2575**).

Here are two specific ways to fix it:

### Option 1: Create a new dedicated group (Recommended)
This is the cleanest method if you only want to authorize this specific contact for use with this agent.

**Add this to your configuration:**
```json
"peer_groups": {
  "whatsapp_authorized": ["zmedia", "+18589522575"]
}
```
*Then, use the channel name `whatsapp_authorized` in your request.*

### Option 2: Update an existing group
If you want to use one of the groups I found in my memory (like `whatsapp_contacts`), you must add the number and the agent to that specific list.

**Modify your existing entry like this:**
```json
"peer_groups": {
  "whatsapp_contacts": ["zmedia", "+18589522575", "other_previously_authorized_numbers..."]
}
```
*Then, use the channel name `whatsapp_contacts` in your request.*

### Summary of what to check:
1.  **The Agent Name:** Ensure the list contains `"zmedia"`.
2.  **The Target Number:** Ensure it includes `"+18589522575"` exactly as you are typing it in the command.
3.  **The Channel Reference:** The string used in your `using <channel>` command must match the key name provided in the `peer_groups` object (e.g., if the key is `whatsapp_authorized`, use `whatsapp_authorized`).


----

[channels]

[channels.whatsapp.zwhatsapp]
#peer_group = "whatsapp_contacts"
enabled = true
mode = "personal"
session_path = "~/.zeroclaw/data/sessions/whatsapp.session.db"

# Peer group routing
group_policy = "allowlist"
allowed_groups = ["AC7604BC299644921E7E305E465D883C@g.us"] # Replace with your specific WhatsApp group JID

dm_policy = "allowlist"
allowed_numbers = ["+18589522575", "+18583808555"]
external_peers = ['zmedia', "+18583808555", "+18589522575"]
approval_timeout_secs = 600
excluded_tools = []

[peer_groups.whatsapp.zwhatsapp]
enabled = true
members = [ 'zmedia', '+18589522575', '+18583808555', "admin", "researcher"]
external_peers = ['zmedia', "+18583808555", "+18589522575"]
targets = ['+18589522575']
#agents = ["zmedia"]
openclaw cron add --name "wa-test" --at "+2m" --delete-after-run --announce --channel whatsapp --to "<phone>" --session isolated --timeout-seconds 60 --message "test"
